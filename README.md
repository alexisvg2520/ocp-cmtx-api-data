# cmtx-api-data
Servicio **DATA** (Python) que lee el mensaje desde MongoDB y lo expone vía HTTP.

## Qué incluye
- Dockerfile + app mínima en Python
- Deployment + Service (Helm)s
- Env/Secret para conexión a Mongo
- CI/CD: build imagen + deploy Helm